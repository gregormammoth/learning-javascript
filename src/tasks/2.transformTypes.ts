function transformToString (anyType) {
  let stringVariable = anyType;
  return stringVariable;
}

function transformToNumber (anyType) {
  let numberVariable = anyType;
  return numberVariable;
}

function transformToBoolean (anyType) {
  let booleanVariable = anyType;
  return booleanVariable;
}

export { transformToString, transformToNumber, transformToBoolean };
