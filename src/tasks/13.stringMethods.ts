export function getUpperCase (str) {
  return str;
}

export function getLowerCase (str) {
  return str;
}

export function getPartOfString(str, start, finish) {
  return str;
}

export function getIndexOfSubstring (str, substring) {
  return str;
}

export function getCapitalizedString(str) {
  return str;
}

export function getTruncatedString(string, limit) {
  return string;
}
